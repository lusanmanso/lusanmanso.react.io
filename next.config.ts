import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['deisishop.pythonanywhere.com'], // Lista de dominios permitidos para imágenes
  },
};

export default nextConfig;